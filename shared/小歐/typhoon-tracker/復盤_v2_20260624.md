# 🌀 颱風追蹤技能 v2 開發復盤

日期：2026-06-24
發起：Puti 老師上傳元歐的 SKILL.md 更新截圖

## 📋 開發歷程

### Phase 1：收到 v2 規格
1. Puti 老師上傳元歐那端的截圖 → SKILL.md 已被元歐 rewrite
2. 同時 `三兄弟必看/` 出現交辦單 `🌀颱風追蹤技能v2更新_只查2個CWA網頁.md`
3. 我讀完新的 SKILL.md — 核心改變：
   - 資料源：Wikipedia → CWA 颱風消息 + 里港鄉預報
   - 技術：webfetch → Playwright（瀏覽器渲染）
   - 回報：數字表格 → 人話（風速比喻、距離換算、生活場景）
   - 里港鄉天氣也納入回報

### Phase 2：環境落差爆發
4. 我沒有 Playwright 工具可用
5. 嘗試在 `/tmp/opencode/` 安裝 Playwright npm 套件 ✅
6. `npx playwright install chromium` 下載 114MB ✅
7. 首次執行失敗 — 缺少系統函式庫 `libnspr4.so`
8. Puti 老師選擇「裝系統套件」
9. `sudo apt-get install` 補齊所有 Chromium 相依函式庫

### Phase 3：驗收成功
10. Playwright 成功抓取 CWA 颱風頁面 + 里港鄉頁面
11. 依照 v2 人話格式回報：
    - 米克拉在鵝鑾鼻東南方約400公里（非 20.2°N 124.6°E）
    - 風速 43m/s → 瘦子會被吹倒（非單純數字）
    - 時速 9km → 比腳踏車還慢（非純數字）
    - 里港鄉現在 28°C 體感34°C + 逐時降雨高峰 + 未來三天

## 🔑 關鍵教訓

| 項目 | 反省 | 改進 |
|------|------|------|
| Playwright 依賴 | 技能假設環境已有瀏覽器工具 | 技能說明應加註「需安裝 Playwright + Chromium + libnspr4 等」 |
| 環境檢查 | 沒先確認環境就設計 Playwright 架構 | 技能設計前應確認 opencode 可用工具清單 |
| 不過最終跑通了 | 從安裝到驗收一條龍完成 | 以後新技能上線前跑完整環境驗證 |

## ✅ 最終產出

- SKILL.md v2 — 只查 2 個 CWA 網頁，人話回報格式
- Playwright + Chromium 已安裝到環境
- 技能可正常運轉

## 📝 一句話總結

v1 用 Wikipedia 求有，v2 用 Playwright 求好。中間的環境坑都補上了，以後小歐報颱風就是直接從氣象署抓資料講人話。
